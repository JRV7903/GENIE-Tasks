experiment_name: "jet_classification_v1"
data:
  filepath: "/kaggle/input/datasets/[ID]/quark-gluon-data/quark-gluon_data-set_n139306.hdf5"
  batch_size: 128
  max_samples: null
  normalize: true
  split_ratio: 0.8
training:
  ae_epochs: 20
  gnn_epochs: 30
  learning_rate: 0.001
  weight_decay: 0.0001
  device: "cuda"
model:
  latent_dim: 128
  hidden_channels: 64
